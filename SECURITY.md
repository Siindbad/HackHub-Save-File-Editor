# Verification Report

Latest release snapshot generated from `dist/security-report.txt`.

No release security snapshot is published yet in this branch.

Run a release publish to refresh this file automatically:

`powershell -NoProfile -ExecutionPolicy Bypass -File tools/publish_public.ps1 -Build -Message "Release vX.Y.Z dist update"`
